# spring.jpa.hibernate.ddl-auto=create
spring.jpa.hibernate.ddl-auto=none
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.datasource.initialization-mode=always

# spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/ndc
# spring.datasource.username=ndc
# spring.datasource.password=ndc

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.database=POSTGRESQL
spring.jpa.database=H2
# spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# create database mmartofel
# create user springuser with password 'ThePassword';
# GRANT CONNECT ON DATABASE mmartofel TO springuser;
# GRANT USAGE ON SCHEMA public TO springuser;
# GRANT ALL PRIVILEGES ON DATABASE mmartofel TO springuser;


spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=sa
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Enabling H2 Console
spring.h2.console.enabled=true
 
# Custom H2 Console URL
spring.h2.console.path=/h2
